# ATLAS content (`content/atlas/`)

**Purpose:** ATLAS-specific **static** research assets (e.g. covariance priors under `covariance/`), experiential descriptor packs (`descriptors-v1.json` for self-nomination), not PCMS question JSON.

**Do not put here**

- PCMS question banks (`content/questions/` outside `atlas-v1/`).
- Generated per-user exports or caches (use storage / export pipelines instead).

**Governance:** **ADR-004** (covariance prior location and offline computation). [`docs/DECISIONS.md`](../../docs/DECISIONS.md).
